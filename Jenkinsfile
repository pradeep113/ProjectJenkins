node{
	stage('Build'){
		echo"Building ..."
		echo "I am now running ${env.BUILD_ID} in ${env.JENKINS_URL}"
		}
	stage('Test'){
		echo"node-name:${NODE_NAME},job-name:${JOB_NAME},build-id:${BUILD_ID},build-tag:${BUILD_TAG}"
		}
	stage('Deploy'){
		echo"It is Deploying..."
		}
	}
