node{
	stage('Build'){
		echo"Building ..."
		echo "I am now running ${env.BUILD_ID} in ${env.JENKINS_URL}"
		}
	stage('Test'){
		echo"It is Testing..."
		}
	stage('Deploy'){
		echo"It is Deploying..."
		}
	}
